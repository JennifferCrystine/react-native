class Estoque {

	contructor () {
		var itensDeEstoque = [];
	}

	adicionaItemEstoque(produto) {
		itensDeEstoque.push(produto);
	}

	retiraItemEstoque(produto) {
		if(itensDeEstoque.splice(produto, 1)) {
			item = itensDeEstoque.splice(produto, );
		}
		

	}
}